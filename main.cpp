#include <iostream>
#include <string>
#include "tinyxml2.h"
#include "Authenticator/Authenticator.h"

int main() {
    Authenticator AuthHandler(2);
    std::cout << "Authentication is " << std::endl;
    return 0;
}
